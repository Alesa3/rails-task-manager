<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%=  @task.completed%></p>

<p><%= link_to "Destroy this task", task_path(@task), method: :delete, data: { confirm: "Are you sure?"} %></p>

<%= link_to "Edit your task", edit_task_path(task) %>

<%= link_to "Back to list", tasks_path %>

